/* Layout for the safe mode test-rule popup (safemode::test_pattern).
   Palette/border/scrollbar/.scroll-pane and the .panel frame come from theme.rcss
   (linked first in safemode_test.rml); this file owns only this popup's layout.
   A centered box over the rules screen behind it; the cursor row gets the accent.
   Lengths in dp. */

body {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 100%;
}

.smt-panel {
    width: 62%;
    height: 78%;
    display: flex;
    flex-direction: column;
}

.smt-title {
    flex: 0 0 auto;
    text-align: center;
    padding-bottom: 4dp;
    margin-bottom: 4dp;
    border-bottom: 1dp {{bg4}};
}

.smt-list {
    flex: 1;
    min-height: 0;
}

.smt-row {
    display: flex;
    flex-direction: row;
    padding: 2dp 6dp;
    border-radius: 4dp;
}

.smt-row:hover {
    background-color: {{bg2}};
}

.smt-row.selected {
    background-color: {{yellow}};
    color: {{bg-hard}};
}

.smt-num {
    flex: 0 0 48dp;
}

.smt-name {
    flex: 1;
}

.smt-footer {
    flex: 0 0 auto;
    text-align: center;
    margin-top: 4dp;
    color: {{red}};
}
