/* Layout for the armor-layers / sort-armor screen (Tier 2: show_armor_layers_ui).
   Palette/border/scrollbar/.scroll-pane come from theme.rcss (linked first). This
   file owns the 4-pane layout. Rows + lines are display:block (RmlUi divs default
   to inline — see the theme.rcss gotcha). */
body {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 100%;
}

.sa-panel {
    width: 96%;
    height: 94%;
    display: flex;
    flex-direction: column;
}

/* Cat header: << name >> + right-aligned hint keys. */
.sa-cat {
    flex: 0 0 auto;
    display: flex;
    flex-direction: row;
    align-items: center;
    margin-bottom: 6dp;
}
.sa-catname {
    color: #fabd2fff;
    margin: 0 6dp;
}
.sa-prev, .sa-next {
    color: #ebdbb2ff;
    padding: 0 4dp;
}
.sa-prev:hover, .sa-next:hover {
    color: #fbf1c7ff;
    background-color: #504945ff;
}
.sa-hint {
    flex: 1;
    text-align: right;
    color: #ebdbb2ff;
}

/* Three equal-ish columns. */
.sa-main {
    flex: 1;
    min-height: 0;
    display: flex;
    flex-direction: row;
}
.sa-col {
    min-height: 0;
    display: flex;
    flex-direction: column;
}
.sa-leftcol  { flex: 0 0 31%; margin-right: 8dp; }
.sa-midcol   { flex: 1;       margin-right: 8dp; }
.sa-rightcol { flex: 0 0 31%; }

/* Pane headers/footers (Innermost / Outermost / column captions). */
.sa-head, .sa-foot {
    flex: 0 0 auto;
    color: #a89984ff;
    display: flex;
    flex-direction: row;
}
.sa-head .sa-r { flex: 1; text-align: right; }
.sa-foot { margin-top: 2dp; }

/* LEFT worn list. */
.sa-list {
    flex: 1;
    min-height: 0;
}
.sa-row {
    display: flex;
    flex-direction: row;
    padding: 0 4dp;
    border-radius: 4dp;
}
.sa-row:hover { background-color: #504945ff; }
.sa-row.selected {
    background-color: #fabd2fff;
    color: #1d2021ff;
}
/* Item picked up for reordering (move mode). */
.sa-row.moving {
    border-left: 2dp #d3869bff;
    background-color: #504945ff;
}
.sa-hide { flex: 0 0 auto; width: 12dp; color: #8ec07cff; }
.sa-name { flex: 1; }
.sa-vol  { flex: 0 0 auto; margin-left: 6dp; color: #a89984ff; }

.sa-protect {
    flex: 0 0 auto;
    margin-top: 4dp;
    white-space: pre-wrap;
}

/* MIDDLE: item detail (scrolls) over the encumbrance/warmth table (fixed). */
.sa-mid {
    flex: 1;
    min-height: 0;
}
.sa-encumb {
    flex: 0 0 auto;
    margin-top: 4dp;
    padding-top: 4dp;
    border-top: 1dp #7c6f64ff;
}
.sa-encumb-head { color: #fbf1c7ff; margin-bottom: 2dp; }

/* RIGHT layering list. */
.sa-right {
    flex: 1;
    min-height: 0;
}

.sa-line {
    display: block;
    white-space: pre-wrap;
}
.sa-msg {
    display: block;
    color: #83a598ff;
}
