#ifdef COOP_ENABLED

#include "coop_hud.h"

#include "cursesdef.h"

namespace coop_hud {

auto draw(const catacurses::window& /*w*/) -> void {
    // TODO: Phase 12 — render partner status panel (name, HP bar, activity)
}

} // namespace coop_hud

#endif // COOP_ENABLED
